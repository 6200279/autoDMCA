# Lightweight requirements for local development
# Core API functionality only - no AI/ML or heavy dependencies

# Core FastAPI and ASGI
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Database - AsyncPG as primary driver (better Windows compatibility)
sqlalchemy[asyncio]==2.0.23
alembic==1.13.0
asyncpg==0.29.0
greenlet==3.0.1

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
bcrypt==4.1.2
cryptography==41.0.7
pyotp==2.9.0
email-validator==2.1.0
user-agents==2.2.0

# HTTP Client & Web Scraping
httpx==0.25.2
requests==2.31.0
aiohttp==3.9.1
aiohttp-retry==2.8.3
beautifulsoup4==4.12.2
selenium==4.15.2

# Background Tasks & Caching
celery==5.3.4
redis==5.0.1
apscheduler==3.10.4

# Email Support
emails==0.6.0
jinja2==3.1.2

# Basic Utilities
python-dotenv==1.0.0
python-dateutil==2.8.2
python-whois==0.8.0
dnspython==2.4.2
slugify==0.0.1
fuzzywuzzy==0.18.0
python-levenshtein==0.12.2
textblob==0.17.1

# File Upload & Storage
aiofiles==23.2.0

# Payment Processing
stripe==7.8.0

# Basic Image Processing (lightweight)
pillow==10.1.0
numpy==1.25.2
imagehash==4.3.1

# Monitoring (lightweight)
sentry-sdk[fastapi]==1.38.0
structlog==23.2.0

# Note: AI/ML packages removed for local development:
# - opencv-python (60MB+)
# - dlib (requires complex build)
# - face-recognition (heavy dependency)
# - torch/torchvision (500MB+)
# - scikit-image (large)
# - numpy (included with pillow)
# 
# Note: Development/testing packages removed:
# - pytest, locust, bandit, mypy, etc. (not needed for runtime)
#
# These will be available in production via requirements.txt